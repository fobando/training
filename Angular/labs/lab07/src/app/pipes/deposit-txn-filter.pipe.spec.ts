import { DepositTxnFilterPipe } from './deposit-txn-filter.pipe';

describe('DepositTxnFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new DepositTxnFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
